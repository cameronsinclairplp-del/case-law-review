# macOS
.DS_Store
._*

# editor / tooling
.vscode/
.idea/
.claude/
*.log

# scratch
/tmp/
*.tmp

# python
__pycache__/
*.pyc

# local project notes — kept out of the public repo (contain operational detail)
/HANDOFF.md

# local secrets — never commit (API keys etc.)
.env
*.env
pipeline/.env

# Judgment source files (copyright — never publish; used locally for ingest)
/Cases/

# Personal study notes (kept out of the public repo)
/MUST-KNOW-CASES.md
/HANDOFF-COWORK.md
